services:
  redis:
    image: redis:7.4-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_volume:/data
    command: redis-server --requirepass 123456qwerty
volumes:
  redis_volume:
    driver: local
